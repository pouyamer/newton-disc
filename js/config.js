const config = {
  canvas: {
    size: { width: innerWidth, height: innerHeight },
    background: "#051724"
  },
  disc: {
    color: {
      saturation: 100,
      lightness: 60
    },
    slices: 6,
    radius: 400
  }
}
